---
title: "2.5 环的局部化"
description: "抽象代数 I —— 环的局部化"
---

# 2.5 环的局部化

接着讲理想似乎有些枯燥。我们换个新鲜而有趣的话题——环的局部化。什么是局部化呢？我们用一句话来做引子：请问整数是怎么变成有理数的？这个问题，越是数学的新手越觉得简单。其实，这背后是有奥秘的。我们无论怎样定义出无限循环阿贝尔群——整数群，无论怎样定义上面的乘法使其成为我们熟知的整数环，都不能简单地说有理数的结构是自然得到有理数域就是整数环的商域的。其实，有理数的定义就是比值的形式，而我们一定要模掉一些等价关系，例如$2 1\sim                                  (2.180)$

4 2我们马上就知道，因为整数环是个整环，因此用某种方法定义出来的结构是个美妙的域（在整数环的例子中，给出的就是有理数域）

。而一般地，对于交换环，我们也能构造出一个由分数的等价类（或在不引起歧义的情况下简化为分数）构成的环。

我们要问三个问题。分子是谁？分母是谁？分数的等价关系怎么定义？下面，我们来通过定义和命题来回答这三个问题。

第一个问题，分子是谁？一般来说，任何一个交换环自身都可以（非交换环的结构太差，很多概念都不方便定义）。

第二个问题，分母是谁？我们当然希望分母可以是 1，这样可以使rr=                   (2.181)

我们当然也希望分母在乘法下封闭，这样才能有ac    ac=                   (2.182)

b d bd而满足这两个条件就可以了——我们要求的不多。事实上，满足这两个条件的，根据第一章第一节所说的，不就是$(R \ {0}, \cdot)$的子幺半群吗？在这里，我们称其为乘法子集。

**定义 2.22**

令$(R, +, \cdot)$是一个交换环，而$S \subset R$。则我们称S是一个乘法子集，若S是$(R \ {0}, \cdot)$的（乘法）子幺半群，即$1\in S                       (2.183)$

$$
\forall a, b \in S, ab \in S                      (2.184)
$$

第三个问题，什么时候两个分数等价？假设乘法子集是S，我们或许希望$a   c\sim   ⇐⇒ ad − bc = 0                     (2.185)$

b d可是上面的式子一般来说不是一个等价关系，因为若a/b = c/d，且c/d = e/ f，我们就只能得到ad = bd，且c f = de，可是从环的结构来说，我们完全不能说明a f = be。因此一般来说，我们修改定义，变成$a   c\sim   ⇐⇒ \exists s \in S, s(ad − bc) = 0                      (2.186)$

b d下面，我们给出完整的定义。

**定义 2.23**

令$(R, +, \cdot)$是一个交换环，而S是乘法子集。则R对S的局部化，记作$(S −1 R, +, \cdot)$，定义为$nr                oS −1 R =    : r \in R, s \in S       \sim          (2.187)$

s其中$r r′\sim      ⇐⇒ \exists t \in S, t (r s' − r' s) = 0      (2.188)$

s s′若$r, r' \in R, s, s' \in S$，我们定义r r' r s' + sr'+ =                                   (2.189)

s s′       ss'r r' rr'=                           (2.190)

s s' ss'

和商环的证明一样，我们把良定义性和环的性质一并放进下一个命题中来证。

**命题 2.26**

令$(R, +, \cdot)$是一个交换环，而S是乘法子集。则R对S的局部化，即$S −1 R, +, \cdot$，是个交换环。

**证明**我们先证明$\sim$是个等价关系，再证明加法和乘法是良定义的，最后证明S −1 R是个环。

第一，我们来证明$\sim$是个等价关系。r/s = r/s是显然的，这是因为$1(r s − r s) = 0(1 \in S)$。假设$r/s \sim r'/s'$，则存在$t \in S$，使得t (r s' − r' s) = 0                                             (2.191)

则(−t)(r' s − r s') = 0                                                  (2.192)

故r'/s' = r/s。最后，如果$r/s \sim r'/s'$，$r'/s' \sim r'′/s'′$，只须证明$r/s \sim r'′/s'′$。我们取$t, t' \in S$，使t (r s' − r' s) = 0                                           (2.193)

t' (r' s'′ − r'′ s') = 0                                            (2.194)

则我们可以通过不断的尝试，凑出一个美妙的 t'′ = tt' s'。于是 t'′ (r s'′ − r'′ s) = 0，这是因为(tt' s')r s'′ = t' s'′ (tr s') = t' s'′ (tr' s) = ts(t'r' s'′) = ts(t'r'′ s') = (tt' s')r'′ s        (2.195)

由于S是乘法子群，故$t'′ = tt' s' \in S$。接下来，即使局部化中的每个元素实际上是等价类，我们还是为了方便起见，用等于号来代替所有的等价号。

第二，我们来证明加法和乘法是良定义的。假设$r 1 r 1′\sim'                                                       (2.196)$

$$
s1  s1r 2 r 2′\sim'                                                       (2.197)
$$

s2  s2故存在$t, t' \in S$，使得t (r 1 s1′ − r 1′ s1 ) = 0                                            (2.198)

t (r 2 s2′ − r 2′ s2 ) = 0′(2.199)

我们只须证明$r 1 r 2 r 1 s2 + r 2 s1 r 1′ s2′ + r 2′ s1′   r′ r′+   =               \sim        ''         = 1′ + 2′                                             (2.200)$

$$
s1 s2        s1 s2             s1 s2           s1 s2′r r  '    r′ r′r1 r2 r1r2\cdot     =         \sim 1′ 2′ = 1′ \cdot 2′                                                 (2.201)
$$

s1 s2 s1 s2            s1 s2 s1 s2根据重新分组，对于$tt' \in S$，我们有tt' (r 1 s2 + r 2 s1 )s1′ s2′ − (r 1′ s2′ + r 2′ s1′ )s1 s2                                (2.202)

′                                                           =tt       (r 1 s1′ − r 1′ s1 )s2 s2′ + (r 2 s2′ − r 2′ s2 )s2 s1′                            (2.203)

=0 + 0 = 0                                                                                   (2.204)

根据拆项补项，同样对于$tt' \in S$，我们有tt' r 1 r 2 s1′ s2′ − r 1′ r 2′ s1 s2                                                               (2.205)

′                                                                                 =tt       r 1 r 2 s1′ s2′ − r 1′ r 2 s1 s2′ + tt'     r 1′ r 2 s1 s2′ − r 1′ r 2′ s1 s2               (2.206)

                                     =tt' r 1 s1′ − r 1′ s1 r 2 s2′ + tt' r 2 s2′ − r 2′ s2 r 1′ s1′                                      (2.207)

=0 + 0 = 0                                                                                             (2.208)

第三，我们来证明S −1 R是个环。根据定义，加法和乘法的封闭性和交换律是显然的。而加法单位元是0/1，乘法单位元是1/1，因为对于任何$r/s \in S −1 R$，我们有0 r 0s + 1r r+ =                =                                   (2.209)

$$
1 s             1s        s1 r 1r r\cdot =          =                                   (2.210)
$$

1 s 1s s乘法的结合律是显然的，而加法的结合律也很简单，我们很容易检验                                                             r1 r2     r 3 r 1 s2 s3 + s1 r 2 s3 + s1 s2 r 3 r 1     r2 r3+     +    =                                 =    +     +                (2.211)

s1 s2     s3              s1 s2 s3               s1     s2 s3加法的逆元也是显然的。r/s 的加法逆元当然是 (−r)/s。

最后，我们只须证明乘法对加法的分配律。令$r 1 /s1 , r 2 /s2 , r 3 /s3 \in S −1 R$，则我们很容易检验$    r1 r2 r3     r 1 (r 2 s3 + r 3 s2 ) r 1 r 2 s3 r 1 r 3 s2 r 1 r 2 r 1 r 3\cdot   +    =                       =          +          =   \cdot   +   \cdot         (2.212)$

s1 s2 s3            s1 s2 s3         s1 s2 s3 s1 s2 s3 s1 s2 s1 s3综上所述，我们就证明了 R 对 S 的局部化 S −1 R 是个交换环。

在一般的交换环上的局部化似乎过于复杂。我们来看一个在整环上的简化版本。

**命题 2.27**

令$(R, +, \cdot)$是一个整环，而S是一个乘法子集，则$a   c\sim   ⇐⇒ ad − bc = 0                                      (2.213)$

b d

**证明**先证必要性。假如ad − bc = 0，那么我们取s = 1，则$1(ad − bc) = 1 \cdot 0 = 0$，这就证明了

$$
a    c\sim                                 (2.214)
$$

b d再证充分性。假如a/b = c/d，则存在$s \in S$（于是$s \neq 0$），使得s(ad − bc) = 0。可是因为R是个整环，而且$s \neq 0$，所以ad − bc = 0。

这就证明了这个命题。

这个命题的意义在于，把我们熟知的分数相等的条件，在整环的情况下重新展示了一遍。

接下来，我们要讲一个重要的命题。那就是说，假如 R 是一个整环，而 S = R \ {0}，则 S −1 R 是一个域，被称为 R 上的分式域。事实上，有理数域就是整数环的分式域。

**定义 2.24**

令$(R, +, \cdot)$是一个整环，我们定义R上的分式域，记作Frac(R)，定义为S −1 (R)，其中S = R \ {0}。

**命题 2.28**

令$(R, +, \cdot)$是一个整环，则R上的分式域Frac(R)是个域。

**证明** 令 S = R \ {0}。

我们已经证明了Frac(R) = S −1 R是个交换环，因此只须证明对任意非零元素$r\in S −1 R                                              (2.215)$

s我们都能找到逆元即可。

而这是因为由$r\neq 0                                     (2.216)$

s我们可以得知$r \neq 0$。

因此，$s\in S −1 R                                 (2.217)$

r而且r s s r sr 1=   =  = =1                                   (2.218)

s r r s sr 1这就证明了 Frac(R) 是个域。

因此，在一个整环 R 中，每一个由乘法子集 S 给出的 S −1 R，都是 Frac(R) 的子环。

在实际应用中，乘法子集 S 除了是整个 R \ {0}，还有两种常见的可能。

第一种是由某个非零元素生成的子幺半群，即$s \in R \ {0}$，而$S = \langle s\rangle = {1, s, s2 , \cdots , }                (2.219)$

第二种是由某个素理想的补集得到的，即 𝔭 ◁ R 是个素理想，而S = R \𝔭                              (2.220)

我们称这样的局部化为在素理想 𝔭 上的局部化。当然，我们需要一个引理来证明交换环中每一个素理想的补集都是乘法子集，这样才能使这个概念是良定义的。

**引理 2.10**

令$(R, +, \cdot)$是一个交换环，而𝔭 ◁ R是一个素理想，则S = R \ 𝔭是一个乘法子集。

**证明**首先，若$1 \in 𝔭$，则𝔭 = R，可是素理想根据定义是不能等于整个环的。因此$1 \in S$。

接着，假设$s1 , s2 \in S$，我们只须证明$s1 s2 \in S$。

因为 s1 ∉ 𝔭，s2 ∉ 𝔭，所以根据逆否命题，s1 s2 ∉ 𝔭                            (2.221)

也就是说，$s1 s2 \in S                            (2.222)$

这就巧妙地证明了素理想的补集是一个乘法子集。