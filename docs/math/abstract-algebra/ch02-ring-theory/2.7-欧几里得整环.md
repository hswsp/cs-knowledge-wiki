---
title: "2.7 欧几里得整环"
description: "抽象代数 I —— 欧几里得整环"
---

# 2.7 欧几里得整环

$h\sqrt     i$在这一节中，我们要补充上一节中没有证明的有关Z                             −5的事实，并且将欧几里得整环从逻辑上插在域和主理想整环之间。这就是说，欧几里得整环的性质比主理想整环还要好，但是没有域好。

我们在证明整数环是唯一分解整环的时候用到了带余除法。欧几里得整环和带余除法的推广密切相关。下面，我们给出定义。

**定义 2.36**

令$(R, +, \cdot)$是一个整环，则我们称R是个欧几里得整环，若存在$f : R \ {0} \to N0$，使得对任意$a \in R$，$b \in R \ {0}$，我们都能找到$q, r \in R$，使得a = qb + r                       (2.312)

其中 r = 0 或 f (r) < f (b)。

这里的 f 被称为欧几里得函数。

这里有两个注意点。一个是这里的 q, r 不需要是唯一的。另一个是我们单独把 r = 0 的情况拿出来，而且不

定义 f (0)。在整数环中，余数的条件是

0 ⩽ r ⩽ |b| − 1                         (2.313)

这里实际上将其分为两条，即 r = 0 或 r < |b|。因此，我们立刻证明了整数环是一个欧几里得整环。

**命题 2.42**

整数环 Z 是个欧几里得整环。

**证明**令$f : Z \ {0} \to N0$，定义为

f (x) = |x|                     (2.314)

则根据带余除法，对于任意$a \in Z$，$b \in Z \ {0}$，我们可以找到$q, r \in Z$，使得a = qb + r                 (2.315)

r = 0 或r = |r | < |b|                (2.316)

即r = 0 或 f (r) < f (b)                (2.317)

这就证明了整数环是个欧几里得整环。

实际上，我们也可以证明域上的多项式环是欧几里得整环。对应的欧几里得函数是众所周知的多项式的次数。为了知识的连贯性，我们选择克制所有在环中对多项式的讨论，而将多项式相关的讨论放到下一章中，因此这个结论也会在下一章中证明。

我们迫不及待地想要证明域是欧几里得整环，而且欧几里得整环是主理想整环。

**命题 2.43**

令$(F, +, \cdot)$是一个域，则F是一个欧几里得整环。

**证明**注意到域的性质是非常好的——每一个非零元素都有乘法逆元。令$a \in F$，$b \in F \ {0}$。取$c \in F$，使得

bc = 1。因此$a = a \cdot 1 = abc + 0 = (ac)b + 0           (2.318)$

我们令 q = ac，r = 0。这就证明了 F 是一个欧几里得整环（无论怎么定义欧几里得函数都可以）。

**命题 2.44**

令$(R, +, \cdot)$是一个欧几里得整环，则R是一个主理想整环。

**证明**令I ◁ R是一个理想。假如I = {0} = (0)，则I显然是个理想。因此我们假设$I \neq {0}$，即$I \ {0} \neq \emptyset$。

因为我们定义的欧几里得函数 f 的陪域是自然数集，而自然数集是有良序公理的。根据良序公理，我们可以找到 I \ {0} 中取值最小的某个 b（不一定是唯一的），使得f (b) = min f (x)                  (2.319)

$x \in I$我们只须证明I = (b)                        (2.320)

首先因为$b \in R$，所以$I \supset (b)$。

假设$a \in I \ (b)$，则a = qb + r                      (2.321)

$$
r\neq 0                        (2.322)
$$

因此f (r) < f (a)。并且$r = a − qb \in I + (b) \subset I               (2.323)$

又因为$r \neq 0$，这就和b的取法相矛盾。

综上所述，我们就证明了每一个欧几里得整环都是主理想整环。

我们进一步补全了环论中的逻辑链条，得到

**命题 2.45**

域 =⇒ 欧几里得整环 =⇒ 主理想整环 =⇒ 唯一分解整环 =⇒ 最大公因数整环 =⇒ 整环 =⇒ 交换环=⇒ 环。

我们在实际情况中，会遇到一种范数。

**定义 2.37**

令$(R, +, \cdot)$是一个整环，我们称$N : R \to R⩾0$是一个范数，若N (x) = 0 ⇐⇒ x = 0                          (2.324)

N (x) = 1 ⇐⇒ x 是一个单位                                (2.325)

$$
\forall a, b \in R, N (ab) = N (a)N (b)                      (2.326)
$$

注意，范数在分析学中有另一种含义。在这一门课程中，在不引起歧义的情况下，我们用“范数”来指代如上所述的类似于同态的映射。$h\sqrt i$例如在高斯整数环$Z[i] = Z −1 = {a + bi : a, b \in Z}$中，我们可以定义N (a + bi) = a 2 + b 2                            (2.327)

则利用复数的性质或直接利用定义，我们都可以证明这个例子中的 N 是一个范数。囿于篇幅，我们将证明留做练习。

考虑到在高斯整数环中$a + bi \in Z[i]$满足$a, b \in Z$，因此$N : R \to N0$是个映射到自然数的函数。我们下面来证明N是一个欧几里得函数函数。

**命题 2.46**

高斯整数环 Z[i] 是一个欧几里得整环。

**证明**令$a = x + iy, b = z + iw \in Z[i] (x, y, z, w \in Z)$。则在复数域C中，我们可以取到

$$
a    x + iy    (xz − yw) + i(xw + yz) xz − yw   xw + yz=        =                        = 2      +i 2      \in Q[i]          (2.328)
$$

b z + iw                 z +w2   2        z +w 2   z + w2其中$Q[i] = {a + ib : a, b \in Q}$。

尽管如此，我们依然可以找到离这两个有理数最近的两个整数，称为$m, n \in Z$，使得xz − yw   1m− 2       ⩽                                                            (2.329)

z + w2    2xw + yz   1n− 2      ⩽                                                            (2.330)

z + w2   2我们令q = m + in                                   (2.331)

是 a/b 的一个很好的（可以是最好的）高斯整数逼近。

而r = a − qb                                   (2.332)

我们只须证明r = 0或者N (r) < N (b)。假设$r \neq 0$，只须证明N (r) < N (b)。而这是因为a                               xz − yw        xw + yzr = a − qb = b    −q = m− 2            +i n− 2                              (2.333)

b             z + w2         z + w2因此 2  21      1     1N (r) ⩽     +      =                                        (2.334)

2      2     2并且因为$b \neq 0$，所以N (b) ⩾ 1 >      ⩾ N (r)          (2.335)

这就证明了这个命题。

实际上，对于映射到自然数的范数，我们有一条很好的性质，即

**命题 2.47**

令$(R, +, \cdot)$是一个整环，$N : R \to N0$是一个范数，而$x \in R$。若N (x)是一个素数，则x是一个不可约元素。

**证明** 用反证法。假设 x = ab，其中 a、b 都是非单位，则根据范数的条件，N (a), N (b) > 1。这就给出了正整数

N (x) 的一个非平凡分解，与 N (x) 是素数的条件相矛盾。

综上，我们就证明了这个命题。

现在，我们来证明上一节中留下的问题。

**引理 2.22**

$$
h\sqrt iN : Z −5 \to N0 ，   \sqrt   N a + −5b = a 2 + 5b 2          (2.336)
$$

$h\sqrt     i$是Z        −5上的一个范数。

$$
\sqrt       h\sqrt i
$$

**证明**令$x = a + −5b \in Z −5$。

第一条是显然的。若 a 2 + 5b 2 = 0，则 a = b = 0。

我们来证明第三条。不难发现$   \sqrt           \sqrt                   \sqrt  2N a + −5b = N a + i 5b = a 2 + 5b 2 = a + i 5b               (2.337)$

就是复数上的模长平方，故 N 显然是乘性映射。

我们来证明第二条。利用一点点数论，N (x) = 1当且仅当$x = \pm 1$。$\pm 1$显然是单位。我们只须证明，若x是个单位，则$x = \pm 1$。假设$             \sqrt       \sqrtxy = a + −5b c + −5d = 1                (2.338)$

两边同时取范数，得到N (xy) = N (x)N (y) = 1            (2.339)

因此$N (x) = \pm 1$，故$x = \pm 1$。

$i        h\sqrt−5$上的一个范数。

综上所述，我们就证明了N是$Z\sqrt       \sqrt$现在，我们来证明上一节留下的练习，即3, 2 + −5, 2 − −5是这个环中的不可约元素。

**引理 2.23**

$\sqrt       \sqrt      h\sqrt i3, 2 + −5, 2 − −5$是Z −5中的不可约元素。

**证明** 一方面，我们计算得到 N (3) = 9，因此如果 3 可以分解成两个非单位 x, y 的乘积，即 3 = xy，则必须有

$$
N (x) = N (y) = \pm 3               (2.340)
$$

然而显然对于任意$a, b \in Z$，我们有$a 2 + 5b 2 \neq 3                (2.341)$

$h\sqrt    i$这就导致了矛盾。因此3是环Z              −5中的不可约元素。

另一方面，我们计算得到$   \sqrt N 2 \pm −5 = 22 + 5 = 9           (2.342)$

$\sqrt       \sqrt        h\sqrt i$同理，2 + −5, 2 − −5也是环Z −5中的不可约元素。

**命题 2.48**

$h\sqrt iZ −5$不是一个唯一分解整环。

**证明**因为$   \sqrt    \sqrt $

9 = 32 = 2 + −5 2 − −5            (2.343)

$\sqrt       \sqrt      h\sqrt i            h\sqrt i$利用上一条引理所说的，3, 2 + −5, 2 − −5是Z −5中的不可约元素。因此，Z −5不是一个唯一分解整环。

**命题 2.49**

$h\sqrt i$在Z −5中，存在是不可约元素却不是素元素的元素。

**证明**例如3是不可约的，而且$   \sqrt    \sqrt $

3|9 = 2 + −5 2 − −5              (2.344)

然而$\sqrt3 \nmid 2 \pm −5                 (2.345)$

$\sqrt$这就证明了3不是素元素。类似地，我们也可以证明$2 \pm −5$是不可约的，却不是素元素。