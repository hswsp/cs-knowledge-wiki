---
title: "1.4 正规子群"
description: "抽象代数 I —— 正规子群"
---

# 1.4 正规子群

我们在这一节会定义一个群的正规子群。正规子群首先是个子群，但是又满足更多的性质。因此正规子群一定是子群，反过来不一定。

假设H < G，我们的一个重要的目标是赋予$G/H = {aH : a \in G}$以群结构。我们当然希望对于$a, b \in G$，定义$(aH) \cdot (bH) = (ab)H                                    (1.67)$

可是注意到一个陪集的表达式不唯一的，根据上一节的引理，若$a, b \in G$，则$aH = a' H ⇐⇒ a −1 a' \in H                                (1.68)$

我们希望上面的乘法是良定义的，所以要给子群一些额外的条件，变成所谓的正规子群。这额外的条件便是正规性，即左右陪集全部相等。我们指的是，

**定义 1.29**

令$(G, \cdot)$是一个群，且$N \subset G$。我们称N是个正规子群，记作N ◁ G，若N是个子群(1.69)

$$
\forall a \in G, aN = Na                                   (1.70)
$$

首先要证的便是上面的陪集乘法是良定义的。

**命题 1.36**

令$(G, \cdot)$是一个群，且N ◁ G，$a, b \in G$，则$(aN) \cdot (bN) = (ab)N                                 (1.71)$

是良定义的。

**证明** [证法一]假设aN = a' N, bN = b' N，则$a −1 a', b −1 b' \in N$，我们只须证明abN = a' b' N，即(ab) −1 a' b' =

$b −1 a −1 a' b' \in N$。首先中间这个部分，即a −1 a'，是在N中的。接着，利用N是个正规子群，我们可以得到b −1 N b = N，因此，$b −1 a −1 a' b' \in b −1 N b' = N$。进一步地，abN = a' b' N。这就证明了良定义性。

**证明** [证法二]事实上，这个乘法可以简单地理解成子集乘法，即$(aN)(bN) = {xy : x \in aN, y \in bN }$。我们只须

说明，这从集合意义上，等于abN。而这几乎是显然的。由于N b = bN，我们有aN bN = abN N = abN。其中最后一步的N N = N，一边是因为乘法封闭性，$N N \subset N$；而另一边是因为乘法单位元，$N = Ne \subset N N$。这样，既然从集合意义上相等，那么自然就是良定义的（因为我们不必选取单位元）。

以上两个证明各有利弊。当然，都理解是最好的。下面，我们要说一个重要的结论，这个结论也是我们期待的，那就是，当 N ◁ G 时，G/N 在这个陪集乘法下构成群，称为商群。

**命题 1.37**

令$(G, \cdot)$是一个群，且N ◁ G，则$(G/N, \cdot)$构成一个群，称为（G在N上的）商群，其中的单位元是eN = N，每个陪集aN的逆元是a −1 N。

**证明** 事实上，我们会发现，良定义性是最难证的。一旦证明了良定义性，利用乘法的定义，上面这些结论几乎

都是显然的。我们一条条来证。

封闭性：在讲陪集的时候已经证过了。

结合律：令$a, b, c \in G$，则利用乘法的定义，(aN bN)cN = (abN)(cN) = ((ab)c)N。利用G对乘法的结合律，得到这是等于(a(bc))N的。类似地，这最终等于aN (bNcN)。

单位元：令$a \in G$，则aNeN = (ae)N = aN，类似地eNaN = aN。

逆元：令$a \in G$，则aNa −1 N = (aa −1 )N = eN，类似地a −1 NaN = eN。

综上，若 N ◁ G，则 G/N 在这个自然的乘法下构成群，称为一个商群。

我们这里补充一个引理，帮助我们更好地判别正规子群。

**引理 1.4**

令$(G, \cdot)$是一个群，且N < G，则下列命题等价1. N是G的正规子群，即$\forall a \in G, aN = Na                                          (1.72)$

$$
2.\forall a \in G, aNa −1 \subset N                                        (1.73)
$$

$$
3.\forall a \in G, \forall n \in N, ana −1 \in N                                   (1.74)
$$

**证明** 显然第二个条件和第三个条件等价。我们只要证明第一个条件与第二个条件等价即可。

一方面，我们假设N是G的正规子群。令$a \in G$，则aN = Na。同时右乘a −1并取一半的包含关系，我们得到了$aNa −1 \subset N$。

 −1另一方面，我们假设第二个条件。令$a \in G$，则由$aNa −1 \subset N$得到$aN \subset Na$，由$a −1 N a −1                                \subset N$得到$Na \subset aN$。因此，aN = Na。

事实上，第二个条件和第三个条件都很好用，唯独第一个条件很少用。

一个简单的命题是，一族正规子群的任意交还是正规子群。

**命题 1.38**

令$(Ni )i \in I$是一族G的正规子群，则它们的交集仍然是G的正规子群，即ÙNi ◁ G                                                        (1.75)

$$
i \in I
$$

**证明** 首先，我们可以知道子群的任意交是子群。这个证明与子集可以生成子群的证明是几乎完全一致的，故这

里略去证明。检验三条或精简的两条即可。

$Ñ                        −1 \in Ñ$因此我们只需要检查正规性。我们利用引理的第三条。令$a \in G, n \in                         i \in I Ni$，我们只须证明$ana            i \in I Ni$。

Ñ                    Ñ任取$i \in I$，则$n \in Ni$。由于Ni ◁ G，我们有$ana −1 \in Ni$。因此，$ana −1 \in           i \in I Ni$。这就证明了$i \in I Ni ◁ G$。

那么十分类似地，读者也可以证明，每一个子集都可以生成一个正规子群，定义为所有包含那个子集的正规子群的交集，而这个交集仍然是一个正规子群。在这里，我们一笔带过，感兴趣的读者可以花一点时间证明。

我们应当举一些正规子群的例子。首先，对于任意群$(G, \cdot)$，平凡子群{e}和整个群G都是正规子群，即

**命题 1.39**

令$(G, \cdot)$是一个群，则{e} ◁ G                      (1.76)

G ◁G                       (1.77)

**证明** 我们实际也没有给过它们是子群的证明，这里一并给了。

平凡群：怎么乘都是单位元，所以对乘法封闭；包含单位元；唯一的元素的逆元还是单位元；在这个群中，a 的左右陪集都是 a{e} = {e}a = {a}。因此，{e} ◁ G。

整个群：子群是显然的；在整个群G中，每个元素的左右陪集都是全集，即aG = Ga = G，这是因为$a \in G$。

因此，G ◁ G。

而对于阿贝尔群而言，子群就是正规子群，正规子群也就是子群，这是因为其乘法是交换的，因此左陪集当然，作为结论，等于右陪集。

**命题 1.40**

令$(G, \cdot)$是个阿贝尔群，则子群就是正规子群，正规子群也就是子群，即H < G ⇐⇒ H ◁ G                       (1.78)

**证明**证明只需一行。根据交换律，$aH = {ah : h \in H} = {ha : h \in H} = Ha$。

到这里，我们已经万事俱备了，是时候介绍群同构第一定理了。这个定理可以从每一个群同态中都找到一个群同构，而其中的一个群就是商群（这就是为什么我们必须先说商群。）

**定理 1.2** (群同构第一定理)

令$f : G \to G'$是一个群同态，则ker( f ) ◁ G，且G在ker( f )上的商群同构于im( f )，即$G/ker( f ) \sim eq im( f )                (1.79)$

特别地，若f是满同态，则$G/ker( f ) \sim eq G'                 (1.80)$

若f是单同态，则$G/{e} \sim eq G \sim eq im( f )                  (1.81)$

若 G 是有限群，则|G |= | im( f )|            (1.82)

|ker( f )|

**证明**这三条推论都是显然的，唯一要说明的是G/{e}为什么同构于G。这是因为$a{e} \mapsto\to a$是一个同构（很容

易检验其既是双射也是同态），这就意味着我们只须证明原命题即可。

首先要说明每个同态的核都是定义域的正规子群。要注意，同态的像未必是正规子群，往往只是普通的子群。我们只须证明，若$a \in G$，$n \in ker( f )$，则$ana −1 \in ker( f )$。注意到f (ana −1 ) = f (a)e' f (a) −1 = e'        (1.83)

因此$ana −1 \in ker( f )$。这就证明了ker( f ) ◁ G。

接下来，我们要找到一个从商群G/ker( f )到像集im( f )的同构映射。我们称这个映射叫$f˜ : G/ker( f ) \toim( f )$，对于$a \in G$，定义为f˜(a ker( f )) = f (a)                 (1.84)

为了方便起见，在不会引起歧义的情况下，我们令 N = ker( f )，也即f˜(aN) = f (a)                     (1.85)

考虑到陪集代表元的不唯一性，我们要证明良定义性。假设aN = a' N，或$a −1 a' \in N$，只须证明f (a) = f (a')，而这是因为f (a') = f (aa −1 a') = f (a) f (a −1 a') = f (a)e' = f (a)                                        (1.86)

这就证明了良定义性。

接下来，我们要证明 f˜ 既是同态，也是双射（单射 + 满射）。

同态：令$a, b \in G$，则f˜(aN) = f (a)，f˜(bN) = f (b)，而f˜ ((aN)(bN)) = f˜(abN) = f (ab) = f (a) f (b) = f˜(aN) f˜(bN)                                                 (1.87)

这就证明了 f˜ 是一个同态。

单射：只须证明ker f˜ = {N }。假设f˜(aN) = e'，则根据定义，f (a) = e'，故$a \in ker( f ) = N$，所以aN = N，这就证明了f˜是一个单射。

满射：令$a' \in im( f )$，取$a \in G$使得a' = f (a)。因此，f˜(aN) = f (a) = a'，这就证明了f˜是一个满射。

综上所述，f˜是一个从商群G/ker( f )到像集im( f )的同构。作为结论，$G/ker( f ) \sim eq im( f )                                                            (1.88)$

至于推论，我们在开头已经证过了。这就完成了整个命题的证明。

fG′Gim( f )

$$
\cdots\cdotker( f )                                                                      e′               \cdot\cdote\cdots\cdot\cdot\cdot\cdots\cdot\cdot\cdot\cdota3 H                             Ga2 H                                                                                                              G/ker( f )
$$

$a7 H                              f˜a4 H                                 \cdots\cdots\cdot\cdota5 H         eH                    \cdots\cdot\cdots\cdots\cdots\cdot\cdot                                                                           \cdota6 H\cdots\cdots\cdots\cdot\cdot                                                                       \cdotam H$图1.5:群同构第一定理示意图.我们举一个例子，你知道一般线性群G L (n, R)在特殊线性群SL (n, R)中的商群是什么结构吗？现在你知道了，因为$det : G L (n, R) \to R\times                                                            (1.89)$

是个满同态，且ker(det) = SL (n, R)，则由群同构第一定理，我们有$G L (n, R)/SL (n, R) \sim eq R\times                                                           (1.90)$

讲了群同构第一定理，不讲第二、第三定理好像有点说不过去。后面两个定理的难度都会低很多，所以不要被数量吓到了。下面，我们来讲剩下两个定理。

**定理 1.3** (群同构第二定理)

令$(G, \cdot)$是一个群，且N ◁ G，H < G。则$H \cap N ◁ H$，N ◁ HN，且$H/(H \cap N) \sim eq HN/N                               (1.91)$

这和之前两个子群乘积的阶的公式是类似的。

**证明**第一，要证明$H \cap N ◁ H$。令$h \in H$，而$x \in H \cap N$，则$hxh−1 \in H$，而且因为N ◁ G，$hxh−1 \in N$，因此

$$
hxh−1 \in H \cap N。
$$

第二，要证明N ◁ HN。令$hn \in HN$，而$n' \in N$。则$hnn' (hn) −1 = h(nn' n−1 )h−1 \in hN h−1 = N$。

第三，要证明$H/(H \cap N) \sim eq HN/N$。令$f : H \to HN/N$，定义为f (h) = hN                                   (1.92)

这显然是良定义的同态（因为N ◁ G）。根据$HN/N = {hnN : h \in H, n \in N } = {hN : h \in H}$，这还是个满同态。

接下来，它的核是$ker( f ) = {h \in H : hN = eN } = {h \in H : h \in N } = H \cap N$。因此，根据群同构第一定理，$H/(H \cap N) \sim eq HN/N                                  (1.93)$

这就证明了群同构第二定理。

注意到，实际上我们曾经在计算有限群的阶的时候，暗示过这个定理。不过当时两边尚且不一定是群，又怎么能同构呢。

**定理 1.4** (群同构第三定理)

令$(G, \cdot)$是一个群，且N ◁ G，M ◁ G，M < N。则N/M ◁ G/M，且$(G/M)/(N/M) \sim eq G/N                               (1.94)$

**证明**首先根据定理，$N/M \subset G/M$。我们要注意N/M是个群，是要证明的。我们只须证明M ◁ N。这几乎是

显然的。令$n \in N$，$m \in M$，则$nmn−1 \in M$。因此N/M是个群。

因为这两个都是群，所以当然有N/M < G/M。接下来我们可以先证明正规性（只利用同态的核也可以证明，不过下面的证明也是便于读者理解正规子群的概念，故保留），这也几乎是显然的。令$nM \in N/M (n \in N)$，$gM \in G/M (g \in G)$，则$(gM) (nM)(gM) −1 = (gng −1 ) M \in {nM : n \in N } = N/M                (1.95)$

因此 N/M ◁ G/M。

那么，我们要定义$f : G/M \to G/N$，定义为f (gM) = gN                                   (1.96)

要证明良定义性。假设gM = g' M，则$g −1 g' \in M$，故$g −1 g' \in N$，所以gN = g' N。

同态是显然的：

(gMg' M) = f (gg' M) = gg' N = gNg' N = f (gM)g(g' M)          (1.97)

满同态几乎也是显然的。任取$gN \in G/N (g \in G)$，则f (gM) = gN。

最后，核是什么呢？$ker( f ) = {gM : f (gM) = gN = eN } = {gM : g \in N } = N/M             (1.98)$

根据群同构第一定理，这就告诉我们$(G/M)/(N/M) \sim eq G/N                                  (1.99)$

综上所述，我们就证明了群同构第三定理。