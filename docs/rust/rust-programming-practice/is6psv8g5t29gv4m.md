# 环境准备

在 macOS 上安装 Rust 可以通过以下步骤进行：
## 使用 `rustup` 安装 Rust
`rustup` 是 Rust 的官方安装工具，提供简单的安装和管理 Rust 版本的功能。
##### 打开终端并运行以下命令：
## 按照提示操作
执行上述命令后，会出现安装提示。可以选择默认安装或自定义安装。按提示继续即可。
## 配置环境变量
安装完成后，你需要将 Rust 的二进制目录添加到你的 `PATH` 环境变量中。通常，安装程序会给出具体的命令，但通常可以在终端中执行以下命令来添加：
你可以将其添加到 `~/.bash_profile` 或 `~/.zshrc` 文件中，以便在每次打开终端时自动加载：
## 验证安装
可以通过以下命令验证 Rust 是否安装成功：
如果安装成功，命令会输出 Rust 编译器的版本信息。
## 更新 Rust（可选）
要更新 Rust 到最新版本，可以使用以下命令：
按照这些步骤，你就可以在 macOS 上顺利安装和使用 Rust 了！