# 复制状态机

共识算法通常是基于复制状态机（Replicated State Machine, RSM）模型。根据这种模型，可以在一系列服务器上根据相同状态的状态机计算出相同的结果副本，即使在一些服务器宕机是也可以继续操作。复制状态机被用来解决分布式系统中各式各样的容错问题。

## 抽象定义

状态机是由一组描述状态的变量和一组修改变量的命令组成的软件抽象，它的状态只取决于它的初始状态和修改这些状态的命令序列。复制状态机是这样一种抽象，它可以使得多个状态机以相同的顺序执行相同的命令， 输出相同的结果，具有相同的状态变化过程，达到相同的最终状态。如果这些状态机位于多个不同的进程中，则实现了状态机跨进程的复制和容错。

下面抽象定义了复制状态机的接口和特性：

复制状态机抽象定义了两个接口事件，一个是输入事件`<Execute|comman d＞`，上层应用调用该接口请求每个进程执行命令 `command`；另一个是输出事件`<Output|result＞`， 表示状态机执行完命令`command`后且输出了结果`result`。

注意复制状态机抽象并不要求串行操作。也就是说，某个进程可以不停地调用输入事件`<Execute|command＞`， 向各个进程中连续发布多个命令，而不必等待上一个输入事件中的命令被执行完毕输出结果。

RSM1一致特性要求每个正确的进程以相同的顺序输出结果， 但并不保证 "先请求、先执行"。也就是说，命令被请求的顺序可能与实际被执行的顺序不同。 例如， 命令command1先于命令command2被请求， 但command1对应的结果result1可能晚于command2对应的结果result2被输出。

RSM2终止特性要求状态机在执行每个命令时是无阻塞的，从而不会因为执行命令被阻塞而导致实现复制状态机的算法不满足**活性**。

## 结构模型

下图是复制状态机的结构（引用自 [《In Search of an Understandable Consensus Algorithm(Extended Version)》](https://pages.cs.wisc.edu/~remzi/Classes/739/Spring2004/Papers/raft.pdf)）， 它由**共识模块、日志模块、状态机**组成。通过共识模块保证各个节点日志的一致性，然后各个节点基于同样的日志、顺序执行指令，最终各个复制状态机的结果实现一致。

![图片](http://blog.mrcroxx.com/posts/paper-reading/raft-extended/figure-1.png)

如上图所示，复制状态机通常使用多副本日志实现，每个服务器存储一个包含一系列指令的日志，状态机会按照顺序执行日志。**每个日志包含相同顺序的相同指令**，因此每个状态机会处理相同的指令序列。因为状态机是确定的，每个状态机都会计算出相同的状态并得出相同的输出序列。

其中保持多副本日志的一致性是共识算法的任务。服务器上的共识模块会接收来自客户端的指令，并将其添加到它的日志中。它与其它服务器上的共识模块通信来确保每个日志最终包含相同顺序的相同请求，即使一些服务器故障也是如此。一旦指令被恰当地多副本化，每个服务器的状态机就可以按日志顺序处理它们，并将输出返回给客户端。这样，所有服务器对外会表现为单个高可靠性的状态机。

警告⚠️
 
在基于复制状态机实现的分布式存储系统中，**Raft 等一致性算法它只能确保各个节点的日志一致性**，也就是图中的流程二。
 
而对于流程三来说，server 从日志里面获取已提交的日志条目，将其应用到状态机的过程，跟 Raft 算法本身无关，属于 server 本身的数据存储逻辑。
 
**也就是说有可能存在 server 应用日志条目到状态机失败，进而导致各个节点出现数据不一致。但是这个不一致并非 Raft 模块导致的，它已超过 Raft 模块的功能界限。**
