---
title: "括号匹配问题"
description: "301. Remove Invalid ParenthesesGiven a string s that contains parentheses and letters, remove the minimum number of invalid parentheses to make the..."
---

# 括号匹配问题

[301. Remove Invalid Parentheses](https://leetcode.cn/problems/remove-invalid-parentheses/)
Given a string s that contains parentheses and letters, remove the minimum number of invalid parentheses to make the input string valid.

Return all the possible results. You may return the answer in any order.

Example 1:

Input: s = &quot;()())()&quot;
Output: [&quot;(())()&quot;,&quot;()()()&quot;]

Example 2:

Input: s = &quot;(a)())()&quot;
Output: [&quot;(a())()&quot;,&quot;(a)()()&quot;]

Example 3:

Input: s = &quot;)(&quot;
Output: [&quot;&quot;]
# BFS算法求解
## 为何用BFS而不是DFS

利用BFS理解起来要远远比DFS要简单的多，因为这道题说的是删除最少的括号！！，如果我们每次只删除一个括号，然后观察被删除一个括号后是否合法，如果已经合法了，我们就不用继续删除了啊。因此我们并不需要将遍历进行到底，而是层层深入，一旦达到需求，就不再深入了。

## 如何进行BFS

我们做BFS，上一层level和下一层level之间的关系为：把所有上一层level中的每个元素都拿出来，列举出在删除一个括号后的所有可能的情况。(不管删除以后是否合法），添加到下一个level中的元素。

例如：current level是 `[&quot;(()&quot;, &quot;())&quot;]`

-  那么下一层level中的元素应该是: 
- 对 &quot;(()&quot; 删除一个括号的所有可能为： (), (), ((
- 对 &quot;())&quot; 删除一个括号的所有可能为： (), )), ()

这六个就是下一个level的全部内容了。

## 如何解决重复的问题

可是我们发现问题，就是有重复的元素出现。 很简单，我们把level中的list换成set，就避免的重复。

## 如何检查括号是否是个合法括号

如何检查括号是否是一个合法的括号，这是一个简单题，可以用堆栈，也可以维护计数器。

## 代码

```c
class Solution {
    bool isvalid(string s){
        int score = 0;
        for(auto& x: s){
            if(x=='(') score++;
            else if(x==')') score--;
            if(score  removeInvalidParentheses(string s) {
        vector ans;
        unordered_set set;
        set.insert(s);
        while(true){
            for(auto& curs:set){
                if(isvalid(curs)){
                    ans.emplace_back(curs);
                }
            }
            if(!ans.empty()) return ans;
            //没找到结果的话删除一个字符，进行下一轮BFS
            unordered_set next_set;
            for(auto& curs:set){
                int n = curs.size();
                for(int i=0;i0 && curs[i]==curs[i-1]) continue;//连续同样的字符只删除一次
                    if(curs[i]=='('||curs[i]==')'){
                        string trim_str = curs.substr(0,i)+curs.substr(i+1,n-(i+1));
                        next_set.insert(trim_str);
                    }
                }
            }
            set = next_set;
        }
        
    }
};
```

# DFS算法求解

## 基本思路

我们知道所有的合法方案，必然有左括号的数量与右括号数量相等。

首先我们令左括号的得分为 1；右括号的得分为 −1。则会有如下性质：

-  对于一个合法的方案而言，必然有最终得分为 0； 
-  搜索过程中不会出现得分值为负数的情况（当且仅当子串中某个前缀中「右括号的数量」大于「左括号的数量」时，会出现负数，此时不是合法方案）。 

**剪枝一：**

我们可以预处理出「爆搜」过程的最大得分： 1813718137

**PS.「爆搜」过程的最大得分必然是：合法左括号先全部出现在左边，之后使用最多的合法右括号进行匹配。**

枚举过程中出现字符分三种情况：

-  左括号：如果增加当前 ( 后，仍为合法子串（即 1813718137) 时，我们可以选择添加该左括号，也能选择不添加； 
-  右括号：如果增加当前 ) 后，仍为合法子串（即 1813718137) 时，我们可以选择添加该右括号，也能选择不添加； 
-  普通字符：直接添加。 

**剪枝二：**

使用 Set 进行方案去重，len 记录「爆搜」过程中的最大子串，然后**只保留长度等于 len 的子串。**

我们可以通过预处理，得到最后的「**应该删除的左括号数量**」和「**应该删掉的右括号数量**」，来直接得到最终的 len。

那么怎么得到「**应该删除的左括号数量**」和「**应该删掉的右括号数量**」?

根据合法方案规则，遍历字符串，遇到`(`，应该删除的左括号数量就+1，遇到`)`就应该删除的左括号数量-1。如果应该删除的左括号数量已经为0了，还遇到`)`，表示这个就是应该删掉的右括号，应该删掉的右括号数量 +1。

因此在此基础上，我们可以考虑多增加一层剪枝。

## 代码

```java
class Solution {
    Set set = new HashSet<>();
    int n, max, len;
    String s;
    public List removeInvalidParentheses(String _s) {
        s = _s;
        n = s.length();

        int l = 0, r = 0;
        for (char c : s.toCharArray()) {
            if (c == '(') {
                l++;
            } else if (c == ')') {
                if (l != 0) l--;
                else r++;
            }
        }
        len = n - l - r;
        
        int c1 = 0, c2 = 0;
        for (char c : s.toCharArray()) {
            if (c == '(') c1++;
            else if (c == ')') c2++;
        }
        max = Math.min(c1, c2);

        dfs(0, "", l, r, 0);
        return new ArrayList<>(set);
    }
    void dfs(int u, String cur, int l, int r, int score) {
        if (l  max) return ;
        if (l == 0 && r == 0) {
            if (cur.length() == len) set.add(cur);
        }
        if (u == n) return ;
        char c = s.charAt(u);
        if (c == '(') {
            dfs(u + 1, cur + String.valueOf(c), l, r, score + 1);
            dfs(u + 1, cur, l - 1, r, score);
        } else if (c == ')') {
            dfs(u + 1, cur + String.valueOf(c), l, r, score - 1);
            dfs(u + 1, cur, l, r - 1, score);
        } else {
            dfs(u + 1, cur + String.valueOf(c), l, r, score);
        }
    }
}
```

c++代码

```c
class Solution {
    unordered_set hash_set;
    int n = 0,len, max ;
    string input; 
public:
    vector removeInvalidParentheses(string s) {
        n = s.size();
        input = s;
        int l = 0, r = 0;
        for(auto&c:s){
            if(c=='(') l++;
            else if(c==')'){
                if(l>0)l--;
                else r++;
            }
        }
        len = n - l - r;
        int lnum = 0, rnum = 0;
        for(auto&c:s){
            if(c=='(') lnum++;
            else if(c==')'){
                rnum ++;
            }
        }
        max = min(lnum,rnum);
        dfs(l,r,0,0,"");
        vector out;
        for(auto& res:hash_set){
            out.emplace_back(res);
        }
        return out;
    }

    void dfs(int l, int r, int index, int score, string s){
        //这里注意了，最后一次放结果的时候,index已经+1了，此时index = input.size();
        if(lmax||score

## 复杂度分析

-  时间复杂度：预处理 1813718137 和 1813718137 的复杂度为$ O(n)$；不考虑 score 带来的剪枝效果，最坏情况下，每个位置都有两种选择，搜索所有方案的复杂度为 1813718137；同时搜索过程中会产生的新字符串（最终递归树中叶子节点的字符串长度最大为 n，使用 StringBuilder 也是同理），复杂度为$ O(n)$。整体复杂度为 1813718137 
-  空间复杂度：最大合法方案数与字符串长度呈线性关系。复杂度为 1813718137