---
title: "经典区间 DP 模板题"
description: "题目描述这是 LeetCode 上的「516. 最长回文子序列」，难度为「中等」。Tag : 「动态规划」、「区间 DP」给你一个字符串 s ，找出其中最长的回文子序列，并返回该序列的长度。子序列定义为：不改变剩余字符顺序的情况下，删除某些字符或者不删除任何字符形成的一个序列。示例 1：输入：..."
---

# 经典区间 DP 模板题

# 题目描述

这是 LeetCode 上的**「516. 最长回文子序列」**，难度为**「中等」**。
Tag : 「动态规划」、「区间 DP」

给你一个字符串 `s` ，找出其中最长的回文子序列，并返回该序列的长度。

子序列定义为：不改变剩余字符顺序的情况下，删除某些字符或者不删除任何字符形成的一个序列。

示例 1：

```bash
输入：s = "bbbab"

输出：4

解释：一个可能的最长回文子序列为 "bbbb" 。
```

示例 2：

```bash
输入：s = "cbbd"

输出：2

解释：一个可能的最长回文子序列为 "bb" 。
```

提示：
- 1813718137
- `s` 仅由小写英文字母组成

# 区间动态规划

这是一道经典的区间 DP 题。

**之所以可以使用区间 DP 进行求解，是因为在给定一个回文串的基础上，如果在回文串的边缘分别添加两个新的字符，可以通过判断两字符是否相等来得知新串是否回文。**

也就是说，使用小区间的回文状态可以推导出大区间的回文状态值。

- **从图论意义出发就是，任何一个长度为 **1813718137** 的回文串，****必然由「长度为**1813718137** 」****（比如**1813718137**->**1813718137**）****或「长度为**1813718137** 」（两边插入）的回文串转移而来。**
- **两个具有****公共回文部分****的回文串之间存在拓扑序（存在由「长度较小」回文串指向「长度较大」回文串的有向边）。**

通常区间 DP 问题都是，常见的基本流程为：
- 从小到大枚举区间大小1813718137
- 枚举区间左端点 ，同时根据区间大小1813718137 和左端点计算出区间右端点1813718137
- 通过状态转移方程求1813718137 的值

因此，我们 **定义**1813718137** 为考虑区间**1813718137** 的最长回文子序列长度为多少。**

不失一般性的考虑1813718137 该如何转移。

由于我们的状态定义 **没有限制** 回文串中必须要选1813718137 或者1813718137 。

我���对边界字符1813718137 和 1813718137分情况讨论，最终的 1813718137 应该在如下几种方案中取 1813718137：
-  形成的回文串一定不包含1813718137  和1813718137  ，即完全不考虑1813718137和1813718137 ：
 1813718137 
-  形成的回文串**可能**包含1813718137 ，但**一定不**包含1813718137 ：
 1813718137 
-  形成的回文串**可能**包含1813718137 ，但**一定不**包含1813718137 ：
 1813718137 
-  形成的回文串可能包含1813718137 ，也可能包含1813718137 ，根据1813718137和 1813718137是否相等： 

![Screen Shot 2022-11-15 at 10.17.48 AM.png](https://cdn.nlark.com/yuque/0/2022/png/22382307/1668478740561-8d495429-d34b-4beb-8df0-6cd4262851a0.png)

需要说明的是，上述几种情况可以**确保我们做到「不漏」，但不能确保「不重」**，对于**求最值问题，我们只需要确保「不漏」即可**，某些状态重复参与比较，不会影响结果的正确性。

一些细节：**我们需要特判掉长度为 1 和 2 的两种基本情况**。当长度为1时，必然回文，当长度为 2 时，当且仅当两字符相等时回文。

## 代码：
Java：

```java
class Solution {
    public int longestPalindromeSubseq(String s) {
        int n = s.length();
        char[] cs = s.toCharArray();
        int[][] f = new int[n][n]; 
        for (int len = 1; len ​

我们也可以不枚举区间大小1813718137，而是枚举每一个固定的左边界1813718137对应的所有右边界1813718137, c++代码如下:

```cpp
 int longestPalindromeSubseq(string s) {
        int n = s.size();
        // dp 数组全部初始化为 0
        vector> dp(n, vector(n, 0));
        // base case
        for (int i = 0; i = 0; i--) {
            for (int j = i + 1; j 

- 时间复杂度：1813718137
- 空间复杂度：1813718137