---
title: "红黑树插入篇"
description: "红黑树的插入在一棵AVL树中，我们通过左旋和右旋来调整由于插入和删除所造成的不平衡问题。在红黑树中，可以使用两种方式进行平衡操作：重新着色旋转当红黑树中出现不平衡的状态，我们首先会考虑重新着色，如果重新着色依旧不能使红黑树平衡，那么就考虑旋转。插入操作主要有两种情况，具体取决于叔叔结点的颜色。..."
---

# 红黑树插入篇

![image](https://mmbiz.qpic.cn/mmbiz_png/rSmDLkNsngSnvnBzLOcN1bv8oJOo0kcAXtBJibwnhPqNKYcwFCVJt5iabaqo4bOc9iaTT4mKlqulUnZFQ5ibe2Kyew/640?wx_fmt=png&wxfrom=5&wx_lazy=1&wx_co=1)

![image.png](https://cdn.nlark.com/yuque/0/2024/png/22382307/1704891131456-865b23bf-5c9b-4f78-839a-df49fd342af9.png)

![image](https://mmbiz.qpic.cn/mmbiz_png/rSmDLkNsngSnvnBzLOcN1bv8oJOo0kcAicdaVQ438TzRVLSdhLVXRPWMteIiaickHOicibHrPDM9NbrcH03bwTCy1pg/640?wx_fmt=png&wxfrom=5&wx_lazy=1&wx_co=1)

![image](https://mmbiz.qpic.cn/mmbiz_png/rSmDLkNsngSnvnBzLOcN1bv8oJOo0kcAlJrG7zZ5fmP8GO5I7LuwnJibdRoiaEfBlDpOrq9Ph5gM5eDHicI0ibGw7Q/640?wx_fmt=png&wxfrom=5&wx_lazy=1&wx_co=1)
# 红黑树的插入
在一棵AVL树中，我们通过左旋和右旋来调整由于插入和删除所造成的不平衡问题。在红黑树中，可以使用两种方式进行平衡操作：
- 重新着色
- 旋转
当红黑树中出现不平衡的状态，我们首先会考虑重新着色，如果重新着色依旧不能使红黑树平衡，那么就考虑旋转。插入操作主要有两种情况，具体取决于叔叔结点的颜色。如果叔叔结点是红色的，我们会重新着色。如果叔叔结点是黑色的，我们会旋转或者重新着色，或者两者都考虑。
一个 `NULL` 结点被认为是黑色的，这在上篇已经提到过。
设 x 为新插入的结点。
-  进行标准的 BST 插入并将**新插入的结点设置为红色**。 
-  如果 x 是根结点，将 x 的颜色转化为黑色（整棵树的黑高增加 1）。 
-  如果 x 的父结点 `p` 不是黑色并且 x 不是根结点，则：
a) 如果 x 的叔叔结点 u 是红色。
- 将插入结点 x 的父结点（Parent ）p 和叔叔（uncle）结点 u 的颜色变为黑色；
- 将 x 的爷爷结点（Grand Parent）g 设置为红色；
- 将 `x = x的爷爷结点 g` ，**对于新的 x 重复 2，3两步**。
b) 如果 x 的叔叔结点 u 是黑色，则对于 x、 x 的父结点 p 和 x 的爷爷结点 g有以下四种情况： 
- LL（p 是 g 的左孩子且 x 是 p 的左孩子）
- LR （p 是 g 的左孩子且 x 是 p 的右孩子）
- RR （p 是 g 的右孩子且 x 是 p 的右孩子）
- RL（p 是 g 的右孩子且 x 是 p 的左孩子）

![image](https://mmbiz.qpic.cn/mmbiz_png/rSmDLkNsngSnvnBzLOcN1bv8oJOo0kcA4DibxpropQG0d9Vz5Csr3ibFBLIqAWicdL7oj9QNT5mLIFKxfgmBzNP6Q/640?wx_fmt=png&wxfrom=5&wx_lazy=1&wx_co=1)

![image](https://mmbiz.qpic.cn/mmbiz_png/rSmDLkNsngSnvnBzLOcN1bv8oJOo0kcAlE4NxYHIAPoyNdljennYQTWicoibLNzZJnicegCcC27pzQ1N7p6RGIo8Q/640?wx_fmt=png&wxfrom=5&wx_lazy=1&wx_co=1)

## 插入结点 x 的叔叔结点 u 为红色
无规矩不能说清楚问题，无图不欢，我们做如下约定：

![image](https://mmbiz.qpic.cn/mmbiz_png/rSmDLkNsngSnvnBzLOcN1bv8oJOo0kcAwwC8YicC1ETc90ex7u8WcWhDMeVq3LW8KVzJCv7T7IdNuiaEmHNB5NMA/640?wx_fmt=png&wxfrom=5&wx_lazy=1&wx_co=1)
对于新插入结点 x，我们执行标准的 BST 插入之后，将插入结点 x 着色为红色；如果插入结点不是根结点，且x的父结点 p 为红色结点，分为 a) 和 b) 两种情况处理，我们先看的是 a) 的情况：x 的叔叔结点 u 为红色，如下图所示：

![image](https://mmbiz.qpic.cn/mmbiz_png/rSmDLkNsngSnvnBzLOcN1bv8oJOo0kcAV5gabU8JaibHclicSibyC2CfTltsnk4uHfe9h6VOoawvn6kectjfNh76Q/640?wx_fmt=png&wxfrom=5&wx_lazy=1&wx_co=1)
第一步：将 p 和 u 的颜色设置为 黑色

![image](https://mmbiz.qpic.cn/mmbiz_png/rSmDLkNsngSnvnBzLOcN1bv8oJOo0kcAaPbxQmlp2y4LjzK6A8xhtD0ZoO04basXgUQHARyox3GewIVWGYsAOQ/640?wx_fmt=png&wxfrom=5&wx_lazy=1&wx_co=1)
第二步：将 g 的颜色设置为红色

![image](https://mmbiz.qpic.cn/mmbiz_png/rSmDLkNsngSnvnBzLOcN1bv8oJOo0kcASxibzUUw8pEOicDNfCsbdpicjPo1CMhcSyrMCI82uI8Yo04UN98EMwZ6A/640?wx_fmt=png&wxfrom=5&wx_lazy=1&wx_co=1)
第三步：将 `x = g`，对 g 重复执行算法框架中的 2，3 两步。

![image](https://mmbiz.qpic.cn/mmbiz_png/rSmDLkNsngSnvnBzLOcN1bv8oJOo0kcAUWpDkxluG44mLUNRnqkegfJ30tyovb1Vsmdy2uHcFZicldpXCNhMhFg/640?wx_fmt=png&wxfrom=5&wx_lazy=1&wx_co=1)
不要嫌我烦，我又得说一说 2，3 两步是干嘛的
-  如果 x 是根结点，将 x 的颜色转化为黑色（整棵树的黑高增加 1）。 
 如果 x 的父结点 p 不是黑色并且 x 不是根结点，则：
a).
b). a) 我们已经讲过了，综合来看就如下图所示：

![image](https://mmbiz.qpic.cn/mmbiz_png/rSmDLkNsngSnvnBzLOcN1bv8oJOo0kcAo2s0BkARyQKuXlUu8IJZicTC5R9af7icKNicYswwj8Brbf1PHKmEmL4NA/640?wx_fmt=png&wxfrom=5&wx_lazy=1&wx_co=1)
那我们接下来看看 b) 的情况下该如何处理。
## 插入结点 x 的叔叔结点 u 为黑色
当插入结点 x 的叔叔结点为的时候，根据插入结点 x 、x 的父结点 p 和 x 的爷爷结点 g 可能出现的位置关系，分为以下四种情况：
### LL

![image](https://mmbiz.qpic.cn/mmbiz_png/rSmDLkNsngSnvnBzLOcN1bv8oJOo0kcALcUV1XoVDricmgM7r8yNwNmYyONI9R7eRr17N9Z686icp34vibdURLY9Q/640?wx_fmt=png&wxfrom=5&wx_lazy=1&wx_co=1)
### LR
首先通过 **左旋 p** 转化为 **LL** 的情况：

![image](https://mmbiz.qpic.cn/mmbiz_png/rSmDLkNsngSnvnBzLOcN1bv8oJOo0kcAcQMFk4ibcOjnYAKFsdmXv8r7bjstNmMKZWcGFxjxicZdCibzlvGRY41IA/640?wx_fmt=png&wxfrom=5&wx_lazy=1&wx_co=1)
然后按照 **LL** 的情况处理：

![image](https://mmbiz.qpic.cn/mmbiz_png/rSmDLkNsngSnvnBzLOcN1bv8oJOo0kcA9D6mQAqqiapstvNibudaT8slm6yFC0Gs8Ray3nBRP9Mrtuiag0IY7R6vg/640?wx_fmt=png&wxfrom=5&wx_lazy=1&wx_co=1)
### RR

![image](https://mmbiz.qpic.cn/mmbiz_png/rSmDLkNsngSnvnBzLOcN1bv8oJOo0kcAI4viczaUDoZhD2tpXLlfyEb98bQJeqUCVSpIcdm3LNgCszXWm5ZqPMA/640?wx_fmt=png&wxfrom=5&wx_lazy=1&wx_co=1)
### RL
首先 **右旋 p** 转化为 **RR** 的情况：

![image](https://mmbiz.qpic.cn/mmbiz_png/rSmDLkNsngSnvnBzLOcN1bv8oJOo0kcAicYIfQjDcmXDicscLGtwJUwp8t8TJQ3fXjGMoyfwRtEkJCrgsvR1jngw/640?wx_fmt=png&wxfrom=5&wx_lazy=1&wx_co=1)
然后按照 **RR** 的情况处理：

![image](https://mmbiz.qpic.cn/mmbiz_png/rSmDLkNsngSnvnBzLOcN1bv8oJOo0kcALqia2Kkc7gnNsnWFORy2rDR04AFCibEW03BmwqWUgFdgepYU9T9ISrQg/640?wx_fmt=png&wxfrom=5&wx_lazy=1&wx_co=1)
到这里，插入排序可以说是已经讲完了，但我知道有些朋友难免会感到困惑，那就让我们一起构造一颗自己的红黑树，解决掉所有的困惑吧！
# 红黑树插入操作案例
下面就带大家构造一颗稍微复杂一点儿的红黑树：

![image](https://mmbiz.qpic.cn/mmbiz_png/rSmDLkNsngSnvnBzLOcN1bv8oJOo0kcATwzIU0lhY4dPDFCMicU2eXBpWZlErk51ialRiaqt0VAC9b9EyXJH47sIA/640?wx_fmt=png&wxfrom=5&wx_lazy=1&wx_co=1)
初始时，我们仅已知下面要依次插入的序列：

![image](https://mmbiz.qpic.cn/mmbiz_png/rSmDLkNsngSnvnBzLOcN1bv8oJOo0kcApb2mFLy7e11cbp7vBYsVJ17YaVibRTcvW0hr3hkkvtKGzy344sdKBSw/640?wx_fmt=png&wxfrom=5&wx_lazy=1&wx_co=1)
第一步：插入结点 **2** ，结点 **2** 就是根结点，设置为黑色：

![image](https://mmbiz.qpic.cn/mmbiz_png/rSmDLkNsngSnvnBzLOcN1bv8oJOo0kcArqcbdflA2BIISV0qlp3hqiaAV00yzVlmff8P2RTguqRH96wXVX412cg/640?wx_fmt=png&wxfrom=5&wx_lazy=1&wx_co=1)
第二步：插入结点 **6** ，首先执行标准的 **BST** 插入操作并将结点 **6** 设置为红色，但 **6 号结点的父结点 2 的颜色为黑色，所以什么都不做了**。

![image](https://mmbiz.qpic.cn/mmbiz_png/rSmDLkNsngSnvnBzLOcN1bv8oJOo0kcAQCtJLFrjd3cAlErtALEicXeBzKDiatQHcQtnWia99AGzHTwPBEKFIn6zA/640?wx_fmt=png&wxfrom=5&wx_lazy=1&wx_co=1)
第三步：插入结点 **9（x）** ，执行标准的 **BST** 插入操作并设置为红色，其父结点 **6（p）** 的颜色为红色且且叔叔结点为 NULL结点为黑色，属于 **RR** 的情况，故对其爷爷结点 **2（g）** 进行左旋操作，并交换 **g** 和 **p** 的颜色。

![image](https://mmbiz.qpic.cn/mmbiz_png/rSmDLkNsngSnvnBzLOcN1bv8oJOo0kcA1xZkVxYLPQ96KJB4Jx9wORiaic3rUiaR3cCcwL9SibicN1RP0HTox7yxbyg/640?wx_fmt=png&wxfrom=5&wx_lazy=1&wx_co=1)
第四步：插入结点 **10（x）** ，执行标准的 **BST** 插入操作并设置为红色，其父结点 **9（p）** 为红色，其叔叔结点 **2（u）** 为红色结点，属于 a)的情况，将其父结点 **9** 和叔叔结点涂黑色，并将其爷爷结点涂红色并设置为新的`x`，判断其爷爷结点 **6** ，发现为根结点，重新更新为黑色。

![image](https://mmbiz.qpic.cn/mmbiz_png/rSmDLkNsngSnvnBzLOcN1bv8oJOo0kcADtypyjciatb4F1KfW2LsbHOibgSbnibPNEQljJZMKU7h16aa1dmlT0fsg/640?wx_fmt=png&wxfrom=5&wx_lazy=1&wx_co=1)
第五步：插入结点 **12（x）** ，执行标准的BST插入操作并设置为红色，其父结点 **10（p）** 为红色，其叔叔结点为黑色，其爷爷结点 **9（g）** 为红色，**RR** 的情况，则左旋 **g** ，交换 **g** 和 **p** 的颜色。

![image](https://mmbiz.qpic.cn/mmbiz_png/rSmDLkNsngSnvnBzLOcN1bv8oJOo0kcA2RYkVwaCmIRhPSkbg9EqpNz3pOTxRQvu7RaxNjDibGRe5PQOia6kwxYQ/640?wx_fmt=png&wxfrom=5&wx_lazy=1&wx_co=1)
接下来的步骤我希望小禹禹可以忘记 **x、p、g、u** ，所谓忘记，就是让你沉浸其中，无我，但是所有的过程你又是无比清晰，我想你得 **道** 了。
第六步：插入结点 **15**

![image](https://mmbiz.qpic.cn/mmbiz_png/rSmDLkNsngSnvnBzLOcN1bv8oJOo0kcADVyWoibTAiaOn1ISW8luLVAz0m5kIosyyVA12cRM8UNd5uTlAfbaIbaw/640?wx_fmt=png&wxfrom=5&wx_lazy=1&wx_co=1)
第七步：插入结点 **20**

![image](https://mmbiz.qpic.cn/mmbiz_png/rSmDLkNsngSnvnBzLOcN1bv8oJOo0kcAHIJcV9oL5W3HVAeakwxS8PK8CRJaEnIGQcGd5jZ0HXJHykNbUFdg0g/640?wx_fmt=png&wxfrom=5&wx_lazy=1&wx_co=1)
第八步：插入结点 **18** （注意这里我故意少了一步，要心中补上）

![image](https://mmbiz.qpic.cn/mmbiz_png/rSmDLkNsngSnvnBzLOcN1bv8oJOo0kcAPPdGVibzJbsEiczSgmibAuMibhqdpyyZXwxTyFhfw0HQG2jsP7VaOyyK2g/640?wx_fmt=png&wxfrom=5&wx_lazy=1&wx_co=1)
第九步：插入结点 **1**

![image](https://mmbiz.qpic.cn/mmbiz_png/rSmDLkNsngSnvnBzLOcN1bv8oJOo0kcAx8Sjx73Bb0wic97dQ3MGxMbWwNt8mJ31bqKqOyQf13w63dvfXuUzlRg/640?wx_fmt=png&wxfrom=5&wx_lazy=1&wx_co=1)
第十步：插入结点 **5**

![image](https://mmbiz.qpic.cn/mmbiz_png/rSmDLkNsngSnvnBzLOcN1bv8oJOo0kcAa5quUoWlcaDUoZZR6ULvDTtXpTeicdicpTyy2FDjMgMcAFOfDhQibHdmw/640?wx_fmt=png&wxfrom=5&wx_lazy=1&wx_co=1)
第十一步：插入结点 **13**

![image.png](https://cdn.nlark.com/yuque/0/2024/png/22382307/1704906175351-3e49e5e1-ad72-41a6-8ca8-55b2cda5341d.png)
# 红黑树插入操作的实现
## 结点的定义，增加了父结点指针以及结点颜色标记：

```cpp
struct Node 
{ 
    int data; 
    bool color; 
    Node *left, *right, *parent; 
    
    // 构造器 
    Node(int data) 
    { 
        this->data = data; 
        left = right = parent = NULL; 
        this->color = RED; 
    } 
};
```
## 标准的二叉排序树的插入操作

```cpp
/*标准的二叉排序树的插入操作*/
Node* BSTInsert(Node* root, Node *x) 
{ 
    /* 如果树为空，则插入的结点作为根结点返回 */
    if (root == NULL) 
        return x; 

    /* x的值小于根结点的值，则向左子树走 */
    if (x->data data) 
    { 
        root->left = BSTInsert(root->left, x); 
        root->left->parent = root; 
    } 
    else if (x->data > root->data) 
    { 
        root->right = BSTInsert(root->right, x); 
        root->right->parent = root; 
    } 

    /* 返回插入结点 x 的树根*/
    return root; 
}
```
## 红黑树中结点的左旋操作
红黑树中的左旋操作与AVL树中讲的结点的左旋操作类似，但是这里需要修改结点的父指针，所以情况可能稍微复杂一点儿，但只要你对照着我花费时间给你画的图，理解起来也不是很难。

![image](https://mmbiz.qpic.cn/mmbiz_png/rSmDLkNsngSnvnBzLOcN1bv8oJOo0kcA1LGLv7ew3uHXEkDeDLeQW0Lc7OwvpPnqAIiaO6RfwGCQcukxv3f9uOQ/640?wx_fmt=png&wxfrom=5&wx_lazy=1&wx_co=1)

![image](https://mmbiz.qpic.cn/mmbiz_png/rSmDLkNsngSnvnBzLOcN1bv8oJOo0kcAuW248q9Vgc39OeKdVRtXU757tocqMG4F1xN4QRKyJ2mOTIicm20iaLkA/640?wx_fmt=png&wxfrom=5&wx_lazy=1&wx_co=1)

![image](https://mmbiz.qpic.cn/mmbiz_png/rSmDLkNsngSnvnBzLOcN1bv8oJOo0kcADQt7qLVianicbXOic1tgOia30DichpsvaicDqrJCMYZHlbatbe6qkB0bRJ8A/640?wx_fmt=png&wxfrom=5&wx_lazy=1&wx_co=1)

```cpp
void RBTree::rotateLeft(Node *&root, Node *&g) 
{ 
    Node *p = g->right; 

    g->right = p->left; //g->right = T3

    if (g->right != NULL) //T3 != NULL
        g->right->parent = g; //T3->parent = g

    p->parent = g->parent; 

    if (g->parent == NULL) {
        root = p; 
    }
    else if (g == g->parent->left) {
        g->parent->left = p; 
    }
    else{
        g->parent->right = p; 
    }

    p->left = g; 
    g->parent = p; 
}
```
## 红黑树中结点的右旋操作

![image](https://mmbiz.qpic.cn/mmbiz_png/rSmDLkNsngSnvnBzLOcN1bv8oJOo0kcAQ3I5qmvgyruDm5XYJlokgf59X44HaMickXGYPAfEBCAdR35icBpO3zFg/640?wx_fmt=png&wxfrom=5&wx_lazy=1&wx_co=1)

```cpp
void RBTree::rotateRight(Node *&root, Node *&g) 
{ 
    Node *p = g->left; 

    g->left = p->right; 

    if (g->left != NULL) 
        g->left->parent = g; 

    p->parent = g->parent; 

    if (g->parent == NULL) 
        root = p; 

    else if (g == g->parent->left) 
        g->parent->left = p; 

    else
        g->parent->right = p; 

    p->right = g; 
    g->parent = p; 
}
```
## 由于插入结点造成不平衡时的平衡处理
看这个代码的时候注意配合最前面的算法框架描述会更清楚一些。

```cpp
// 处理由于红黑树结点的插入操作所造成的平衡问题 
void RBTree::fixViolation(Node *&root, Node *&pt) 
{ 
    Node *parent_pt = NULL; 
    Node *grand_parent_pt = NULL; 

    while ((pt != root) && (pt->color != BLACK) && 
        (pt->parent->color == RED)) 
        { 

            parent_pt = pt->parent; 
            grand_parent_pt = pt->parent->parent; 

            /* 情况一: 
   pt的父结点parent_pt 是其爷爷结点grand_parent_pt的左孩子 */
            if (parent_pt == grand_parent_pt->left) 
            { 

                Node *uncle_pt = grand_parent_pt->right; 

                /* Case : a) pt 的叔叔结点 uncle_pt 为红色结点
      只需要调整结点颜色，不需要旋转*/
                if (uncle_pt != NULL && uncle_pt->color == RED) 
                { 
                    grand_parent_pt->color = RED; 
                    parent_pt->color = BLACK; 
                    uncle_pt->color = BLACK; 
                    pt = grand_parent_pt; 
                } 
                else // Case :b) x 的叔叔结点为黑色
                { 
                    /* Case : 2 
    pt 是其父结点的右孩子 
    需要左旋操作 */
                    if (pt == parent_pt->right) 
                    { 
                        rotateLeft(root, parent_pt); 
                        pt = parent_pt; 
                        parent_pt = pt->parent; 
                    } 

                    /* Case : 3 
    pt 是其父结点的左孩子 
    需要右旋操作 */
                    rotateRight(root, grand_parent_pt); 
                    swap(parent_pt->color, grand_parent_pt->color); 
                    pt = parent_pt; 
                } 
            } 

                /* 情况二: 
  pt的父结点parent_pt 是其爷爷结点grand_parent_pt的右孩子 */
            else
            { 
                Node *uncle_pt = grand_parent_pt->left; 

                /* Case : a) pt 的叔叔结点 uncle_pt 为红色结点
      只需要调整结点颜色，不需要旋转*/
                if ((uncle_pt != NULL) && (uncle_pt->color == RED)) 
                { 
                    grand_parent_pt->color = RED; 
                    parent_pt->color = BLACK; 
                    uncle_pt->color = BLACK; 
                    pt = grand_parent_pt; 
                } 
                else // Case :b) x 的叔叔结点为黑色
                { 
                    /* Case : 2 
    pt 是其父结点的右孩子 
    需要左旋操作 */
                    if (pt == parent_pt->left) 
                    { 
                        rotateRight(root, parent_pt); 
                        pt = parent_pt; 
                        parent_pt = pt->parent; 
                    } 

                    /* Case : 3 
    pt 是其父结点的左孩子 
    需要右旋操作 */
                    rotateLeft(root, grand_parent_pt); 
                    swap(parent_pt->color, grand_parent_pt->color); 
                    pt = parent_pt; 
                } 
            } 
        } 

    //如果是根结点，颜色置为黑色
    root->color = BLACK; 
}
```
## 红黑树插入操作代码

```cpp
// 红黑树插入操作 
void RBTree::insert(const int &data) 
{ 
    Node *pt = new Node(data); 

    // 执行标准的BST插入操作
    root = BSTInsert(root, pt); 

    // 进行平衡处理
    fixViolation(root, pt); 
}
```
# 应用案例
## 题目描述
给定一个二叉树，判断它是否是一颗类似于红黑树一样的高度平衡的二叉树。
本题中，一棵类似于红黑树高度平衡的二叉树定义为：
对于每一个结点的最大高度不超过最小高度的2倍。
## 输入输出示例

```plain
    10                                        4
      \                                     /    \
       12                                 1      10
         \                                      /  \
          20                                   6    15
 高度不平衡，10的最大高度为3，最小高度为1       高度平衡，可以是一颗红黑树

          16
        /   \
      5     65
           /   \
          32   75
         /
        30
   高度平衡，可以是一颗红黑树
```
## 题目解析
对于每一个结点，到最远的叶结点的路径长度未超过到最近的叶结点的路径长度的两倍，期望时间复杂度为 ，也就是树中的每一个结点都只能遍历一次。
看到这里，希望你停下来，给你的大脑一个机会，思考一下。
思路，遍历树中的每一个结点，获取结点的最大高度与最小高度，并进行比较，判断是否平衡。我们可以设计一个返回三个值的递归函数，一个布尔值（表示树是否平衡），结点的最小高度和最大高度，其中布尔值可以直接返回，而结点的最小高度和最大高度通过引用传递实现，以便在递归返回时父调用使用这些值。

```cpp
using namespace std; 

struct Node 
{ 
    int key; 
    Node *left, *right; 
}; 

/* 创建一个新的结点 */
Node* newNode(int key) 
{ 
    Node* node = new Node; 
    node->key = key; 
    node->left = node->right = NULL; 
    return (node); 
} 

// 判断二叉树是否与
bool isBalancedUtil(Node *root, int &maxh, int &minh) 
{ 
    // 如果是叶子结点，必然是平衡结点，返回true 
    if (root == NULL) 
    { 
        maxh = minh = 0; 
        return true; 
    } 

    int lmxh, lmnh; // 存储左子树的最大与最小高度 
    int rmxh, rmnh; // 存储右子树的最大与最小高度

    // 检查左子树是否平衡，并设置左子的最大与最小高度
    if (isBalancedUtil(root->left, lmxh, lmnh) == false) 
        return false; 

    // 检查右子树是否平衡，并设置右子树的最大与最小高度 
    if (isBalancedUtil(root->right, rmxh, rmnh) == false) 
        return false; 

    // root 的最大高度为左子树与右子树最大高度的最大值+1
    // root 的最小高度为左右子树的最小高度的最小值 + 1
    maxh = max(lmxh, rmxh) + 1; 
    minh = min(lmnh, rmnh) + 1; 

    // 如果结点的最大高度小于等于最小高度的两倍，返回true
    if (maxh left = newNode(5); 
    root->right = newNode(65); 
    root->right->left = newNode(32); 
    root->right->right = newNode(75); 
    root->right->left->left = newNode(30); 
    isBalanced(root)? cout 
![image](https://mmbiz.qpic.cn/mmbiz_png/rSmDLkNsngSnvnBzLOcN1bv8oJOo0kcAGyk2alxCe3mdFZaaXic9a3XUyCoiajSlD1gWTYohfnib7iaK2XuaIl0ibjA/640?wx_fmt=png&wxfrom=5&wx_lazy=1&wx_co=1)

![image](https://mmbiz.qpic.cn/mmbiz_png/rSmDLkNsngSnvnBzLOcN1bv8oJOo0kcAIU11HzxuKyhmjmob9CIGgTItbuiaKKMbAXX3iaCpmq6w9kLsY1Gwn6Ew/640?wx_fmt=png&wxfrom=5&wx_lazy=1&wx_co=1)

![image](https://mmbiz.qpic.cn/mmbiz_png/rSmDLkNsngSnvnBzLOcN1bv8oJOo0kcAKuWCOicWeYQbCIMvGbGxaiaPArRpNz8ic4Xb19Bbs517zNlSXJ6ceUJQA/640?wx_fmt=png&wxfrom=5&wx_lazy=1&wx_co=1)

![image](https://mmbiz.qpic.cn/mmbiz_png/rSmDLkNsngSnvnBzLOcN1bv8oJOo0kcAtcpKYATgPtBFG9nYNZH3nwrJufH9ZRcBpnaG6Wt7tIibrL0CSicFUzDg/640?wx_fmt=png&wxfrom=5&wx_lazy=1&wx_co=1)

![image](https://mmbiz.qpic.cn/mmbiz_png/rSmDLkNsngSnvnBzLOcN1bv8oJOo0kcAuYSNKoJmrsY4VozEsPf4OJqQ8HaecnictmhaKqsVE4iaW6hzGKsKsKJw/640?wx_fmt=png&wxfrom=5&wx_lazy=1&wx_co=1)

![image](https://mmbiz.qpic.cn/mmbiz_png/rSmDLkNsngSnvnBzLOcN1bv8oJOo0kcAQIcEzNedM1j3mo1AfY6edYMvIG0AOB2gr2HVBZ1pGAhPcJ6MxoNWzg/640?wx_fmt=png&wxfrom=5&wx_lazy=1&wx_co=1)

![image](https://mmbiz.qpic.cn/mmbiz_png/rSmDLkNsngSnvnBzLOcN1bv8oJOo0kcAG9fJriaDct4hMmdkK7Zxfw1d3zwqVVYzuDQW6dic0IfGE95UWGibjLPRg/640?wx_fmt=png&wxfrom=5&wx_lazy=1&wx_co=1)

![image](https://mmbiz.qpic.cn/mmbiz_png/rSmDLkNsngSnvnBzLOcN1bv8oJOo0kcAjAU8NjRdvL0ldxRqSAnMaJe4ENEDPtQGRQNOqWEny708tlJu14854g/640?wx_fmt=png&wxfrom=5&wx_lazy=1&wx_co=1)

![image](https://mmbiz.qpic.cn/mmbiz_png/rSmDLkNsngSnvnBzLOcN1bv8oJOo0kcAhrnMZRB5hMOOgVLuLdyI8LLlN3HDznJ6hojdjzPNNicFMia6AToFdvkg/640?wx_fmt=png&wxfrom=5&wx_lazy=1&wx_co=1)

![image](https://mmbiz.qpic.cn/mmbiz_png/rSmDLkNsngSnvnBzLOcN1bv8oJOo0kcA2XATGv0Yzsq90QiawUpy7uj6ibdhqXP7UWDX2Ov4kl9YXMusY4fLD56Q/640?wx_fmt=png&wxfrom=5&wx_lazy=1&wx_co=1)

![image](https://mmbiz.qpic.cn/mmbiz_png/rSmDLkNsngSnvnBzLOcN1bv8oJOo0kcAKBIpiaDA0bQj3FgnuKnH2mrHIe3Ru2fTqdT0xlHWDoS96QETtDYGw4Q/640?wx_fmt=png&wxfrom=5&wx_lazy=1&wx_co=1)

![image](https://mmbiz.qpic.cn/mmbiz_png/rSmDLkNsngSnvnBzLOcN1bv8oJOo0kcAibSoIXYmfbibHiahDDdnUibHh9WIpq9egyjKBlVdGkPp0EdSttjv57KTcQ/640?wx_fmt=png&wxfrom=5&wx_lazy=1&wx_co=1)